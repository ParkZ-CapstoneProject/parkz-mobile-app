import 'package:flutter/material.dart';

class ParkingSuggestion extends StatelessWidget {
  const ParkingSuggestion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
